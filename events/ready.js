const Discord = require('discord.js')
module.exports = async (client, message) => {
    client.user.setActivity('Message Me -new For Help!', { type: "PLAYING", });
    console.log(`Volcania ModMail is now Running!`)
}